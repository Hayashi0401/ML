# 実装ルール

## 1. ファイルパスの固定化（エラー対策）

実行環境によるパスのズレを防ぐため、スクリプトの先頭で必ず以下を実行し、カレントディレクトリをスクリプトの場所に固定する。

```python
os.chdir(os.path.dirname(os.path.abspath(__file__)))
```

## 2. Flaskのフォルダ構成の厳守

* HTMLファイルは必ず `templates/` フォルダ配下に配置する。
* CSSや画像などの静的ファイルは必ず `static/` フォルダ配下に配置する。
* アップロードされた画像の一時保存には `uploads/` フォルダを使用し、フォルダが存在しない場合はコード内で自動生成する。

```python
os.makedirs("uploads", exist_ok=True)
```

## 3. Git管理のルール（最重要）

AIの学習済みモデル（`.h5` や `.keras` 形式のファイル）や、一時アップロードされた画像（`uploads/` 内のファイル）はファイルサイズが大きいため、絶対にGitにコミットせず、`.gitignore` で除外する。

```gitignore
# 学習済みモデル
*.h5
*.keras

# アップロード画像
uploads/

# 仮想環境
.venv/
venv/
```

## 4. Gitマージルール

複数人が同一機能や関連する機能（例：フロントエンド1とフロントエンド2）を並行して開発している場合は、意図しない競合や不具合を防ぐため、マージ前に第三者によるコードレビューや動作確認を実施すること。

レビューおよび確認が完了した後に、対象ブランチへのマージを行う。


