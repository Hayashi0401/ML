# コーディングルール

## 1. マジックナンバー（謎の数字）を使わない

画像サイズなどの値をコード中に直接記述せず、コードの上部で定数として定義し、使い回す。

### 良い例

```python
IMAGE_SIZE = 200

img = cv2.resize(img, (IMAGE_SIZE, IMAGE_SIZE))
```

### 悪い例

```python
img = cv2.resize(img, (200, 200))
```

値を定数化することで、後から変更する際の修正箇所を減らし、コードの可読性と保守性を向上させる。

---

## 2. コメントを適切に残す

処理の区切りごとに、何を行っているかが一目で分かるコメントを記述する。

### コメント例

```python
# ライブラリのインポート

# ファイルパスの設定

# データの読み込み

# データの分割

# モデルの作成

# モデルの学習

# モデルの評価

# 推論の実行
```

処理内容が明確になることで、コードの理解や保守、共同開発時の可読性向上につながる。
